see i have attached a reference for revamping my producst page with this [dashboard.png](docs-and-refs/dashboard.png) , this is a polymarket website, i also want to build the same page of this dashboard, let me give you more context, on why i want to build this dashboard, bascially i have interview with a company which also launching the same product as polymarket but in south asia, starting from indonesia, so now for round 2 its a frontend case study system design round, which will included these topics, Design Led architecture, web performance, websocket, auth and authorization

so with this revamp i want to solid my internals and understanding on websockets, auth and authorization , primarly and web performance, design led architecture secondarily

So i want you to help me do this, you will act as my instrcutor, you will explain me each and every concept and provide me full code , and help me in writing all the code, you will not do any execution , i will handle all the execution

---

Progress log - 2026-05-05

Current agreed approach:
- Move one step at a time. Do not dump the whole implementation at once.
- User wants to learn the concepts, not just receive finished code.
- Assistant should provide lesson + focused code for the current step only.
- User will apply code and run commands locally.
- Assistant can review code/errors after the user applies changes.

Product goal:
- Replicate the Polymarket dashboard reference as closely as practical because time is short.
- Do not redefine the product/brand or create a new South Asia variant yet.
- Main route target is /products.

Interview prep priorities:
1. WebSockets and real-time market updates.
2. Auth and authorization.
3. Web performance.
4. Design-led frontend architecture.

Architecture decision:
- Keep package.json dev script as "next dev".
- Keep package.json socket script as "node socket.js".
- Next dev server should run on localhost:3000.
- Socket.IO realtime server should run separately on localhost:3001.
- This is clearer for learning: HTTP initial render and WebSocket live updates are separate systems.

Planned learning/build sequence:
1. Architecture picture and mental model.
2. Static Polymarket-like dashboard UI using mock market data.
3. Mock auth layer.
4. Local Socket.IO server on port 3001.
5. Connect dashboard to live updates.
6. Interview explanation and review.

Current status:
- Step 1 architecture reset is complete.
- Next step is Step 2: build only the static dashboard UI.
- No auth, no socket client, no live updates in Step 2.

Step 2 started:
- Goal is a static Polymarket-like /products dashboard.
- This step should not include auth, Socket.IO client code, polling, or live updates.
- Keep package.json scripts unchanged.
- Keep socket.js unchanged for now.
- Use typed mock market data so later WebSocket updates can target stable market/outcome ids.

Step 2 reset after review:
- Use nextjs, next-best-practices, nextjs-best-practices, frontend-design, and web performance guidance for this step.
- websocket-engineer is relevant to the project but deferred until the Socket.IO step.
- ProductPanel should remain a Server Component in Step 2 because there is no useState, useEffect, browser API, or event handler yet.
- app/(products)/page.tsx must be deleted, not redirected, because route groups do not affect URL paths and that file maps to /.
- Static dashboard code should avoid invalid/fragile Tailwind classes and reserve stable dimensions to reduce layout shift.

Step 2 architecture correction:
- Add React skills: react:components and vercel-react-best-practices.
- Use a Next Route Handler to model the mock backend API: app/api/markets/route.ts.
- Do not have the Server Component fetch its own API route over HTTP; instead share a server-side data function between the route handler and the page.
- This teaches the API boundary without adding avoidable internal HTTP overhead or a server-side waterfall.
- Static UI should be split into modular React components with typed Readonly props.

Review checkpoint:
- User kept app/api/markets/route.ts for later use. This is okay as an optional API boundary.
- Current blocker: app/(products)/products/page.tsx passes dashboard to ProductPanel, but ProductPanel still expects products and maps products.
- Current Step 2 correction needed: ProductPanel should stop being a Client Component for now, remove polling/useEffect/server action import, and accept typed dashboard props.

Step 2.1:
- Replace ProductPanel with a minimal typed Server Component smoke test.
- ProductPanel receives Readonly<{ dashboard: MarketDashboard }>.
- Remove "use client", polling, useEffect, ProductItem, and getProductById from ProductPanel.
- Expected result: /products renders static market cards from getMarketDashboard().

Step 2.2:
- Next layer is static dashboard shell/header/navigation.
- Keep everything as Server Components.
- Start modularizing UI under components/market-dashboard/ so later auth and socket client boundaries can be introduced surgically.

Step 2.3:
- Add FeaturedMarketCard as a typed Server Component.
- This replaces the smoke-test featured section with a Polymarket-like hero card.
- Still no client components, buttons with handlers, auth, or sockets.

Step 2.4:
- Add MarketSidebar as a typed Server Component for breaking news and hot topics.
- ProductPanel layout becomes two-column on xl screens: main featured content plus sidebar.
- Still no client components or realtime behavior.

Step 2.5:
- Add AllMarketsSection and MarketCard as typed Server Components.
- Use non-interactive visual controls for tabs and Yes/No pills because trading/auth behavior comes later.
- Keep stable card dimensions and clipped titles for layout stability.

Step 2.6:
- Static dashboard implementation should now be complete enough for review.
- Before moving to auth, user should run dev/lint/typecheck and visually inspect /products.
- Next phase after review is Step 3: mock auth with a small Client Component boundary.

Step 2 completed:
- User confirmed everything is fine.
- Proceeding to Step 3.

Step 3.1:
- Build mock auth model only.
- Define users, roles, permissions, and mock token generation.
- No UI, no localStorage, no sockets yet.
- Purpose is to separate authentication identity from authorization permissions before adding client state.

Step 3.2:
- Add AuthSwitcher as the first intentional Client Component boundary.
- It owns browser-only localStorage and role switching.
- Keep DashboardHeader as a Server Component that renders AuthSwitcher as a child.
- AuthSwitcher does not yet control trading behavior; that comes in the next auth step.

Step 3.2 correction:
- ESLint flagged setState inside useEffect in AuthSwitcher.
- Replace useState/useEffect localStorage hydration with useSyncExternalStore.
- This models localStorage as an external store and avoids cascading render lint warnings.

Step 3.3:
- Add authorization-aware trade controls as a small Client Component.
- TradeControls reads the current mock user from the same localStorage-backed external store.
- Guest sees disabled trade affordance; trader/admin see active visual controls.
- No real trade mutation yet and no socket yet.

Step 3.4:
- Add local mock trade feedback inside TradeControls.
- This is not a real mutation and does not touch server/socket.
- Purpose is to demonstrate authorization gating and user feedback before adding backend behavior.

Clarification:
- Auth is not using sockets in Step 3.
- Current auth is localStorage + useSyncExternalStore + permission checks.
- Sockets later will only authenticate the connection handshake; they are not the auth system.

Step 3.5:
- Add a mock trade Route Handler with server-side authorization.
- This uses HTTP POST, not sockets.
- Client-side permission checks are UX only; route handler enforces permission.

Step 3.6:
- User observed broad list re-rendering when placing a trade.
- Optimize the auth hook snapshot: useSyncExternalStore should return a stable primitive userId, not a newly parsed user object.
- Derive the user from mockUsers after reading the primitive snapshot.
- Memoize TradeControls so local trade state only affects the clicked control.

Time-crunch pivot before interview:
- Auth is good enough conceptually: identity -> role -> permission -> client UX + server enforcement.
- Remaining auth cleanup: derive mock token from selected user, and remove/type old ProductItem for typecheck.
- Prioritize WebSocket implementation now because user missed it in prior round.
- WebSocket goal: separate Socket.IO server on localhost:3001, auth handshake, market rooms, periodic market:update events, client reconnects and applies deltas.

Step 4 implementation target:
- Replace socket.js with standalone Socket.IO server on port 3001.
- Add LiveMarketsClient as a focused Client Component for live probabilities.
- ProductPanel remains mostly Server Component: header + sidebar server-rendered, live market area client-rendered.
- Client connects with mock token, joins market rooms, receives snapshot/update events, and merges outcome probabilities by id.

Step 4 debug checkpoint:
- Hydration warning appeared in MarketMiniChart because render-time chart point generation used Math.sin/floating-point values that can differ between server and browser.
- Fix: make chart points deterministic with a fixed integer wave pattern.
- Socket status showed "Authentication required", meaning the Socket.IO server rejected the handshake.
- Fix: ensure socket server is restarted on port 3001, use getToken() from useMockAuth, and log/verify token during handshake if needed.
